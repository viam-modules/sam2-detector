viam-sdk==0.79.1
typing-extensions
numpy
pillow
huggingface-hub
pyinstaller
sam-2 @ git+https://github.com/facebookresearch/sam2.git
# torch and torchvision are installed separately by setup.sh
# with the correct platform-specific index URL (PyPI vs ROCm).
